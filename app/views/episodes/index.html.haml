%h1 Listing episodes

- @roots.each do |episode|
  %ul
    %li
      = link_to episode.name, episode_path(episode)
      %ul
        - episode.children.each do |ep|
          %li= link_to ep.name, episode_path(ep)


-#%table
  %tr
    %th Name
    %th Description
    %th
    %th
    %th

  - @episodes.each do |episode|
    %tr
      %td= episode.name
      %td= episode.description
      %td= link_to 'Show', episode
      %td= link_to 'Edit', edit_episode_path(episode)
      %td= link_to 'Destroy', episode, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Episode', new_episode_path
